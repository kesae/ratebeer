<%= render @style %>
<p>
  <strong>Beers:</strong>
  <ul>
    <% @style.beers.each do |beer| %>
      <li><%= beer.name %></li>
    <% end %>
  </ul>
</p>
<div>
  <%= edit_and_destroy_buttons(@style) %>
  <p><%= link_to "Back to styles", styles_path %></p>
</div>
